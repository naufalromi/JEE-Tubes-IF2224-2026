program ParseTwo;
var
    x, y, z: Integer;
begin
    x := 1;
    y := x + 2;
    z := (y * 3) + (x + 4);
    writeln('Hello, world');
    writeln('Arithmetic and calls');
    writeln(x);
    writeln y;
    writeln(z
end.
