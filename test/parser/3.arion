program ParseThree;
var
    x, y: Integer;
begin
    x := 1;
    y := 0;
    if x > 0 then
        y := x
    else
        y := 1;
    while y < 5 do
        y := y + 1;
    for x := 1 to 3 do
        y := y + x;
    repeat
        y := y - 1
    until y == 0
end.
