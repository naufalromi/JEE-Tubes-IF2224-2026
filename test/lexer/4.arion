program ParseTwo;
var
    x, y, z: Integer;
begin
    x := 1;
    y := x + 2;
    z := y * 3;
    writeln('Hello, world');
    writeln('Parser test');
    writeln(x);
    writeln(y);
    writeln(z)
end.
