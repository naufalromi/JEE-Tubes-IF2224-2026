program Hello;
var
    x, y, z: Integer;
begin
    x := 1;
    y := x + 9;
    z := y * 2;
    writeln('Hello, world');
    writeln('This is a longer parser test');
    writeln(x);
    writeln(y);
    writeln(z)
end.
